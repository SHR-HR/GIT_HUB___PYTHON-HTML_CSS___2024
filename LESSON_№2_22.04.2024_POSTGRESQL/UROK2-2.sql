SELECT DISTINCT city
FROM employees

-- SELECT AVG(unit_price), MAX(unit_price),
-- 	   MIN(unit_price), COUNT(*), SUM(unit_price)
-- FROM products

-- SELECT product_id, product_name, quantity_per_unit, unit_price
-- FROM products
-- WHERE NOT unit_price = 19
-- WHERE product_name IN ('Tofu','Flotemysost','Grandma''s Boysenberry Spread')
-- WHERE product_name NOT IN ('Tofu','Flotemysost','Grandma''s Boysenberry Spread')

-- <>, =
-- <=, >=
-- >, <
-- AND OR
-- BETWEEN

-- + - / * 
-- ORDER BY unit_price 
-- DESC if naoborot

-- LIMIT n first n elems

-- IN
-- NOT IN